const superVillains: string[] = [];
// const villainPower: number[] = []
const villainPower: Array<number> = [];

type User = {
  name: string;
  isActive: boolean;
};

const allUsers: User[] = [];
const MLModels: number[][] = [[255, 255, 255], []];

superVillains.push("Gu Yue Fang Yuan");
villainPower.push(1);

allUsers.push({ name: "", isActive: true });
